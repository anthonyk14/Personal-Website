import Section from '../components/Section';

const Academics = () => {
    return (
        <Section title="Academics">
            <p>No academic information yet. Check back later!</p>
        </Section>
    );
};

export default Academics;
