import Section from '../components/Section';

const Blog = () => {
    return (
        <Section title="Blog">
            <p>No blog posts yet. Check back later!</p>
        </Section>
    );
};

export default Blog;
