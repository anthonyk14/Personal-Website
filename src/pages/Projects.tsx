import Section from '../components/Section';

const Projects = () => {
    return (
        <Section title="Projects">
            <p>No projects yet. Check back later!</p>
        </Section>
    );
};

export default Projects;
