import Section from '../components/Section';

const Publications = () => {
    return (
        <>
            <Section title="Publications">
                <p>No publications yet. Check back later!</p>
            </Section>
        </>
    );
};

export default Publications;
